## Creating the age vector 
age = c(60, 61, 62, 63, 64, 65, 66, 68, 68, 69)

## Computing the mean age
mean(age)

## Computing the variance of age
var(age)

## Computing the mean of the log(age)
mean(log(age))

